{
  "main": "lib/index.js",
  "slug": "common/pex",
  "version": "1.1.4",
  "remote": "StandartHentaPlugins/pex",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "release-it": "yarn version --patch && git push --follow-tags"
  },
  "dependencies": {
    "sequelize": "^5.21.6"
  },
  "devDependencies": {
    "@types/node": "^13.13.0"
  }
}
